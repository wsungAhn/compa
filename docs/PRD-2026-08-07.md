# PRD — COMPA (2026-08-07, v3)

> [snarktank/ralph](https://github.com/snarktank/ralph) `skills/prd`의 구조(Overview/Goals/
> User Stories/FR-N/Non-Goals/Technical Considerations/Success Metrics/Open Questions)를
> 따랐다 — "새 기능"이 아니라 이미 존재하는 제품 전체를 정확히 기술 + 남은 갭을 실행
> 가능한 요구사항으로 명세하는 용도로 적용. 구현 현황·함수 명세는
> `docs/architecture-reference-2026-08-07.md`를 같이 볼 것.
>
> v1(같은 날 오전) 대비 변경: 타깃 사용자를 국적 기준에서 연령/관심사 기준으로
> 정정, 남은 갭 5개를 User Story + FR-N으로 명세, 개발 워크플로 섹션 추가(Fable
> 위임 분석 반영), 신규 아이디어 2개(인플루언서 협찬 신호, GRWM 조합 추출)를
> Open Question으로 등록.
>
> v2(같은 날 오후) 대비 변경(Open Question 답변 회차): 결제는 Stripe로 시작
> 확정(국내 PG는 나중에 병행 추가), 인플루언서 신호는 "협찬 감지" 요건이 빠져
> 기존 소셜 파이프라인 확장으로 재정의 후 US-006으로 승격, GRWM은 저작권/특허
> 리스크 낮음을 확인해 진행 확정(데이터소스 설계는 여전히 Open), 라우팅은
> react-router로 확정.

## 1. Introduction / Overview

제품명을 입력하면 여러 국가·플랫폼의 할인 이력과 **인플루언서 딜 신호**(US-006)를
모아 "지금 살지, 기다릴지"를 추천하는 화장품 가격 비교 서비스. 백엔드(수집·매칭·
예측 파이프라인)가 프론트엔드(단일 화면 SPA)보다 훨씬 앞서 있다는 게 이 PRD의
핵심 전제다 — 이 문서의 절반은 "이미 만든 걸 사용자가 볼 수 있게 하는 것"에
관한 것이다.

## 2. Goals

- 사용자가 "이 가격이 싼가, 다른 데가 싼가, 기다려야 하나"를 한 화면에서 판단할 수
  있게 한다(대부분 이미 됨 — §4).
- 이미 완성된 백엔드 자산(크로스 통화 매칭 검토 큐, 딜 신호 원본, 세일 예측)을
  사용자·운영자가 실제로 접근 가능하게 한다.
- 자가서비스 수익화 경로(구독 결제)를 연다 — 지금은 사전발급 키 방식이라 "구매"가
  불가능하다.
- 소셜 콘텐츠(인플루언서 딜 신호·GRWM)를 가격 비교를 넘어선 발견(discovery) 자산으로
  확장한다 — 딜 신호는 US-006으로 확정, GRWM 조합 추출은 진행 확정이나 데이터소스
  설계는 아직(§10 Open Questions).

## 3. 타깃 사용자 (2026-08-07 정정)

**연령·관심사 기준이지 국적 기준이 아니다.** 1차 고객은 한국이지만, UI/UX가
국가별로 갈라질 이유가 없다 — 국가는 가격 비교의 **입력값**(어느 나라 가격을
비교하는지)이지 사용자 **세그먼트**가 아니다.

- **핵심 타깃**: 성별 무관, **20대 후반~40대**, 스킨케어·화장품에 관심 있는 소비자.
  "직구족"처럼 좁게 규정하지 않는다 — 국내에서만 사는 사람도 "이 가격이 싼가"는
  똑같이 궁금하다.
- 크로스 통화 비교는 이 타깃 안의 **부분집합**(해외구매 관심층)에게 특히 유효한
  기능이지, 별도 세그먼트가 아니다.

## 4. 지금 실제로 서비스되는 기능 (라이브, 코드 직접 확인)

- 제품 검색 → 할인 이력 타임라인 + 가격 추이 차트
- "지금 살지/기다릴지" 추천(WaitBuyWidget) — D-day·다음 세일명·예상 할인율까지
  `core/sale_calendar.py:next_sale()`이 계산해 이미 뜬다
- 플랫폼 간 가격 비교, 크로스 통화 포함(`PriceComparison.tsx`가 "OO에서 N% 더
  저렴해요"를 이미 렌더링 — 갭은 UI가 아니라 매칭 커버리지, §7 FR-4)
- Reddit·Slickdeals 딜 신호 수집(48시간 보존 강제)
- 광고 슬롯 + 프리미엄 키 게이팅(구독 결제 없음)
- 피드백 수집 + 관리자 조회, 제휴 링크 변환(Amazon US·쿠팡·Rakuten)

## 5. User Stories (남은 갭 — 우선순위 순)

### US-001: 결제/구독 자가서비스
**Description:** As a 사용자, I want 카드로 직접 구독을 결제 so that 사전발급 키를
누군가에게 요청하지 않고도 프리미엄을 쓸 수 있다.

**Acceptance Criteria:**
- [ ] **Stripe로 시작**(확정, 2026-08-07) — 국내 PG(토스페이먼츠 등)는 나중에
  **병행 추가**(교체 아님, 카드사/지역별 라우팅 또는 체크아웃에서 선택). 지금
  둘 다 붙이지 않는 이유: 웹훅·정산대사·실패케이스가 그대로 두 배가 된다 —
  Working Skeleton First(하나로 시작, 실제 마찰이 확인되면 두 번째를 얹는다)
- [ ] 구독 상태가 DB에 저장(현재 `premium_api_keys` 정적 목록 방식 폐기)
- [ ] `X-Premium-Key` 인증 경로를 구독 상태 조회로 교체(기존 API 계약 유지)
- [ ] 결제 실패/취소/환불 경로 처리
- [ ] **무인 자동구현 금지** — 결제는 전 과정 유인 리뷰(Fable 권고, §8)

### US-002: 크로스 통화 매칭 검토 화면
**Description:** As a 운영자, I want `product_match_candidates`의 pending 항목을
화면에서 승인/거부 so that curl로 API를 직접 안 두드려도 된다.

**Acceptance Criteria:**
- [ ] `/api/admin/product-matches` 목록을 표로 렌더(양쪽 제품명·브랜드·score 표시)
- [ ] 승인/거부 버튼 → 기존 API 그대로 호출(409 처리 포함)
- [ ] `X-Admin-Secret` 인증(기존 방식 재사용, 새 인증 발명 금지)
- [ ] Verify in browser using dev-browser skill

### US-003: 딜 신호 원본 피드
**Description:** As a 사용자, I want Reddit/Slickdeals에서 온 "지금 화제인 딜"을
직접 볼 so that SaleEvent로 흡수되기 전 원본 신호(할인율·출처·시각)를 놓치지 않는다.

**Acceptance Criteria:**
- [ ] 딜 신호 원본을 노출하는 API 신설(현재 없음 — 저장은 되지만 읽는 경로가 없다)
- [ ] 프론트에 피드 화면(리스트, 최신순)
- [ ] 48시간 보존 정책과 UI가 일관(만료된 딜은 안 보임)
- [ ] Verify in browser using dev-browser skill

### US-004: 매칭 커버리지 가시화
**Description:** As a 운영자, I want 전체 카탈로그 중 크로스 통화 매칭이 끝난 비율을
볼 so that 배치 주기(6시간)가 충분한지 판단할 수 있다.

**Acceptance Criteria:**
- [ ] 고아 상품 수 / 전체 상품 수 지표(관리자 화면 또는 API)
- [ ] 매칭 안 된 대표 상품 목록(디버깅용)

### US-005: 프론트엔드 라우팅 도입
**Description:** As a 개발자, I need 최소한의 라우팅(또는 탭 구조) so that 딜피드·
어드민 화면을 단일 컴포넌트 트리에 억지로 끼워넣지 않아도 된다.

**Acceptance Criteria:**
- [ ] **react-router로 확정**(2026-08-07 — 가장 널리 쓰이는 표준, React 19와
  호환성 좋음, 논쟁거리 아니라 바로 결정)
- [ ] 기존 검색→비교 플로우가 회귀 없이 동작
- [ ] 딜피드(US-003)·어드민(US-002)이 각자 경로를 가짐
- [ ] Verify in browser using dev-browser skill

### US-006: 인플루언서 딜 신호
**Description:** As a 사용자, I want 인플루언서가 인스타/틱톡에 올린 할인코드·
할인링크를 다른 딜 신호(Reddit/Slickdeals)와 같은 자리에서 볼 so that 지금
살까 말까 고민 중인 제품에 쓸 수 있는 코드가 있는지 놓치지 않는다.

**2026-08-07 스코프 정정**: 처음엔 "협찬(유료광고) 여부 감지"로 논의됐으나,
**협찬 여부는 무관하다**로 확정(사용자 확인) — 이 앱을 쓰는 사람은 이미 "사겠다/
필요하다"고 마음먹은 상태에서 스마트한 딜을 찾는 것이지, 광고 여부를 판별해주는
서비스가 아니다. 그러면 이건 **신규 기능이 아니라 기존 소셜 파이프라인
(`app/social/`, `app/ai/extractor.py`, `app/ai/pipeline.py`)의 소스 확장** —
Reddit/Slickdeals에서 "할인코드+가격"을 뽑는 것과 동일한 추출 로직을 인플루언서
게시물에 적용하면 된다.

**Acceptance Criteria:**
- [ ] 인스타/틱톡 게시물에서 제품명+할인코드(또는 링크)+가격 추출(기존
  `extractor.py` 패턴 재사용, 새 AI 파이프라인 발명 금지)
- [ ] 추출된 딜이 US-003(딜 신호 피드)에 같은 형식으로 합류
- [ ] 협찬/광고 여부 판별 로직은 만들지 않는다(Non-Goal, §7)
- [ ] 존재하지 않는 코드/만료된 코드를 걸러내는 최소 검증(가능하면)

## 6. Functional Requirements

- FR-1: 구독 결제 연동은 기존 `X-Premium-Key` 게이트를 대체하되, 그 게이트를 쓰는
  기존 엔드포인트(`/api/products/{id}/events` 등)의 응답 계약은 바꾸지 않는다.
- FR-2: 매칭 검토 화면은 `app/api/admin.py`의 기존 3개 엔드포인트만 쓴다 — 새
  어드민 API를 만들지 않는다.
- FR-3: 딜 신호 피드 API는 기존 SaleEvent/SaleWindow 스키마를 바꾸지 않는다(원본
  신호를 노출하는 새 읽기 전용 API만 추가).
- FR-4: 매칭 커버리지 지표는 `product_match_candidates` + `products` 테이블의
  단순 카운트 쿼리로 충분하다 — 새 집계 테이블을 만들지 않는다.
- FR-5: 라우팅 도입은 기존 컴포넌트(`SearchBar`, `PriceComparison` 등)를 재작성하지
  않고 페이지 컨테이너로만 감싼다.
- FR-6: 인플루언서 딜 신호(US-006)는 협찬/광고 여부를 판별하지 않는다 — 할인
  코드·가격 추출만 한다. 판별 로직을 넣으면 스코프 위반이다.

## 7. Non-Goals (범위 밖)

- 한국·중국 소스의 완전한 크로스 통화 매칭(JP만 라이브 검증됨)
- 실시간 환율(정적 테이블 유지)
- 자체 결제 게이트웨이 신규 구축(기존 PG 연동 우선)
- **인플루언서 협찬(유료광고) 여부 판별** — 사용자에게 중요하지 않은 정보로 확정
  (2026-08-07). US-006은 딜 신호 추출만 한다.
- GRWM 조합 추출의 실제 구현(§10 — 진행은 확정, 데이터소스 설계는 아직 없음)

## 8. 개발 워크플로 (Fable 위임 분석 반영, 2026-08-07)

2026-06-23 Threads 워크플로 제안("역할별 AI 분업 + Codex/Claude Opus 반복검토루프")을
검토한 결과: **compa가 이미 이 제안의 80%를 실행 중**(Tier별 AI 라우팅, GLM 적대감사
+ 리뷰어 diff 검증, Working Skeleton First). 새로 오케스트레이션을 만들지 않는다 —
아래 두 가지만 기존 파이프라인에 추가한다.

1. **PRD를 설계문서보다 먼저 쓴다** — 요구사항이 자명한 항목(스키마가 곧 스펙인
   경우)은 생략 가능하지만, "무엇을 만들 것인가" 자체가 열려 있는 항목(US-002,
   US-003처럼 UI 형태가 정해지지 않은 것)은 `prd` 스킬로 먼저 확정한다.
2. **태스크를 쪼갤 때 항목마다 "완료 판정 명령"을 명시한다**(테스트/스크린샷 등
   기계적으로 pass/fail이 갈리는 명령). 구현자(GLM) 신뢰도가 낮다는 게 D단계에서
   실측됐으므로(중복 태스크 생성, 프로덕션 버그, 테스트 인프라 연쇄 파괴 — 전부
   Claude가 직접 디버깅해서 고침), 판정 명령이 명확하면 리뷰 비용이 "명령 실행"으로
   줄어든다.

**무인 반복 실행(Ralph 스타일) 범위**:
| 백로그 항목 | 무인 루프 | 이유 |
|---|---|---|
| US-002 어드민 UI, US-005 라우팅 | 허용 | API 기존, 기계적 검증(테스트+스크린샷) 가능, 실패해도 무해 |
| US-003 딜피드 | 부분(파이프라인만) | 스크래핑 판단(rate limit 대응)은 유인 필요 |
| US-001 결제 | **금지** | 비가역 외부 부작용, 돈 경로 — 전 과정 유인 리뷰 |

실패 시 왕복 대신 **리뷰어(Claude)가 직접 수정**한다(GLM 왕복 비용이 직접수정보다
컸다는 게 D단계에서 실증됨).

## 9. Success Metrics

- 검색 → 비교 화면 도달률
- WaitBuyWidget 클릭률
- 매칭 커버리지(US-004로 가시화된 뒤 측정 가능)
- 프리미엄 전환율(US-001 이후 측정 가능)
- 어드민 화면 도입 후 검토 큐 처리 시간(현재: curl 수동 운영 기준선 없음 — 도입 후
  측정 시작)

## 10. Open Questions

- ~~결제 수단~~ → **해결(2026-08-07)**: Stripe로 시작, 국내 PG는 필요해지면 병행
  추가(US-001).
- ~~인플루언서 협찬 신호~~ → **해결(2026-08-07)**: 협찬 판별 불필요로 확정, 기존
  파이프라인 확장(US-006)으로 재정의.
- ~~라우팅 라이브러리~~ → **해결(2026-08-07)**: react-router(US-005).
- **GRWM 코스메틱 조합 추출**(2026-08-07 제안, 진행 확정): "하울"이 아니라
  "Get Ready With Me" 영상에서 인플루언서의 실제 제품 조합(루틴)을 추출해 발견
  콘텐츠로 노출한다. 가격비교와는 다른 축(제품 발견/영감)이라 별도 PRD 필요.
  - **저작권/특허 리스크 확인 완료(2026-08-07)**: "화장품 사용 조합"에 걸리는
    특허·디자인등록은 사실상 없다 — 특허는 신청·심사·신규성 요건이 필요한데
    업계에 선행사례가 넘쳐 신규성을 못 채운다. 저작권은 영상/글이라는 **표현**에
    걸리지 "이 사람이 이 제품들을 이 순서로 썼다"는 **사실**엔 안 걸린다(사실은
    저작권 보호 대상이 아니라는 국제적으로 확립된 원칙 — 이 서비스가 이미
    다른 리테일러 가격을 스크래핑하는 것과 같은 논리). 원본 링크를 걸어주는
    현재 방침(사용자 확인)이면 도의적으로도 충분.
  - **남은 것(YouTube 이용약관)**: 스크래핑 관련 제약은 세일캘린더 기능
    (`scripts/refresh_sale_timing.py`, yt-dlp 기반)이 이미 다루는 것과 같은
    종류의 리스크라 새로 생기는 게 아니다.
  - **아직 미정**: 데이터 소스(자막 vs 설명란 vs 직접 시청 후 추출) — 별도
    설계 필요.
- **매칭 배치 주기**: US-004로 커버리지를 실측한 뒤 6시간이 충분한지 재판단.
- **SMS 전용 브랜드 딜 신호**(2026-08-07 제안): Fresh 등 일부 럭셔리 브랜드는
  공개 채널이 아니라 **가입자 SMS로만** 딜을 발송한다 — 스크래핑·제휴 피드
  어디에도 안 잡히는 사각지대. 과거 이력은 수동으로 확보 예정(가족 문자함에서
  복사). **앞으로 자동 수집은 미착수** — 필요한 것: (1) Fresh 외 다른 SMS
  구독 브랜드 목록 확인, (2) 캡처 방식 결정. 개인 기기의 문자를 다루는 문제라
  본인 동의·통제가 되는 방식이어야 한다 — 후보: iOS 단축어(Shortcuts) 자동화
  (특정 발신자/키워드 감지 → 전송, 기기 소유자가 직접 설정·통제) vs Apple
  문자 포워딩(더 무거움, Apple ID 연결 필요). 브랜드 목록·방식 둘 다 정해지기
  전까지 설계 착수 안 함.

## 11. 수익화 우선순위 메모 (2026-08-07, §8 논의 반영)

Goal의 "자가서비스 수익화"를 구독(US-001) 하나로 좁게 읽지 않는다 — 실측해보니
**제휴가 개발비 대비 더 빠른 경로**다:
- 구독의 유일한 코드 상 가치("3년치 이력")는 지금 실데이터가 이틀치뿐이라
  사실상 무의미하다(§4 참고) — **광고 제거**가 훨씬 실질적인 프리미엄 가치이고
  구현비도 거의 없다(클라이언트에서 `AdSlot`을 프리미엄 여부로 숨기면 끝).
- 제휴는 Amazon US·쿠팡·Rakuten 외에 **브랜드 공홈 26곳**에도 열려있다 —
  `docs/affiliate-network-research-2026-08-07.md`에 전수 조사 완료. Impact.com
  하나로 확인된 것만 4개 브랜드(Herbivore, ILIA, Kosas, Natasha Denona)가
  잡힌다 — 코드 작업 없이 계정 신청만으로 여는 수익 경로다.
- **권고 순서**: 제휴 계정 신청(Impact.com 우선) → Amazon 어소시에이트 태그
  발급 → 구독은 그 다음(무료 사용자 수가 쌓여 "이대로는 부족하다"는 신호가
  보일 때).
