# 감사 B r6 — 일일 수집 스코프 설계 (codex gpt-5.5) · **라운드 상한**

- 일시: 2026-08-08 09:4x PDT · 감사자: `codex exec -m gpt-5.5`
- 이번 라운드 추가 지시: ①r5 반영이 새 모순을 만들었나 ②**설계 자체를 공격하는 지적이
  아직 남아있나 — 없으면 없다고 말하라** ③구현자가 이 문서만으로 T1~T16을 구현할 수 있나

| 심각도 | 건수 | 반영 | 기각 |
|---|---:|---:|---:|
| P0 | **0** | — | — |
| P1 | 1 | 1 | 0 |
| P2 | 4 | 4 | 0 |
| P3 | 1 | 1 | 0 |

## 감사자의 자체 총평 (원문)

> "Path A 자체의 큰 방향, 즉 **'브랜드 공홈 sweep + 기존 상품 exact 갱신 + 생성 없음'은
> 방어 가능하다.** 다만 이 문서만으로 T1~T16을 전부 안정적으로 구현하기에는
> `_collect_platform` 반환 계약, exact matcher 구현 방식, DB conflict 테스트 방식,
> 라이브 스모크 재실행 조건이 아직 모호하다."

즉 **설계 방향은 승인, 남은 것은 구현 명세의 정밀도**다.

## P1 — 헬퍼 반환값 변경이 사용자 검색을 지울 수 있다

현행 `_collect_platform`은 저장 후 **insert 여부와 무관하게** `collected_product_ids.add(prod.id)`
한다(`collector.py:296-300`). 새 헬퍼가 `inserted count`를 돌려주므로 구현자가 **양쪽 다**
`inserted > 0`일 때만 계수하기 쉽다 → **오늘 이미 수집돼 중복만 나온 상품이 검색 결과에서
사라진다.**

**반영**: 두 호출부의 계수 조건이 다르다는 것을 표로 명시. `_collect_platform`은 항상 추가,
Path A만 `inserted > 0`. **T17 신설** — 중복만 발생해도 `_collect_platform`이 product를 반환.

## P2 4건

| 지적 | 반영 |
|---|---|
| `_find_exact_for_sweep`의 "normalized exact" 구현 방식 모호 — `normalize_name()`이 HTML·공백까지 건드려 DB 직접 비교로는 후보가 빠진다 | 3단계 고정: ①brand lower + `deleted_at IS NULL`로 DB 후보 좁힘 ②Python에서 `normalize_name` 비교 ③0/1/2+ 분기. 대소문자·중복공백·HTML 태그 테스트 포함 |
| 로그 예시에 `updated`가 없어 r5의 카운터 계약과 어긋남 | 로그 포맷에 `updated=U` 추가 |
| T9/T15를 FakeSession으로는 검증 못 함 (`ON CONFLICT ... RETURNING`과 유니크 인덱스의 실제 동작이 핵심) | **live PG 테스트로 지정**. A(NullPool)에서 쓴 skip 판정 재사용, 스킵을 통과로 보고 금지 |
| 라이브 스모크의 `inserted > 0`이 재실행 불가능한 기준 | 사전값 기반 4단계 판정으로 교체 — 오늘 이미 돌았으면 `inserted == 0`도 정상, `matched ≈ 211` + 행 수 무증가로 판정 |

## P3

`persist_scraped` 잔존 명칭 1건 → `persist_events_for_product`로 통일.

## 수렴 판정 — **미수렴, 라운드 상한 도달**

수렴 기준은 "P0/P1 0건이 2라운드 연속"이었고, **6라운드 내내 한 번도 충족되지 않았다.**

| 라운드 | P0 | P1 | P2 | P3 | 계 | 지적의 성격 |
|---|:-:|:-:|:-:|:-:|:-:|---|
| r1 | 0 | 2 | 4 | 1 | 7 | 설계가 기댄 **함수의 실제 동작** |
| r2 | 0 | 2 | 2 | 2 | 6 | **r1 반영이 만든 자체 모순** |
| r3 | 0 | 2 | 2 | 1 | 5 | 설계가 기댄 **외부 전제** |
| r4 | 0 | 1 | 2 | 1 | 4 | **r3 반영이 만든 자체 모순** |
| r5 | 0 | 2 | 3 | 0 | 5 | **문서 정합성** (설계 공격 0건) |
| r6 | 0 | 1 | 4 | 1 | 6 | **구현 명세 정밀도** (설계 방향은 감사자가 승인) |

**P0는 6라운드 내내 0건이었다.** P1은 매 라운드 1~2건 나왔으나, r5부터 성격이
"설계가 틀렸다"에서 "문서/명세가 덜 정밀하다"로 바뀌었고 r6 감사자가 설계 방향을
명시적으로 방어 가능하다고 판정했다.

**리뷰어 판단: 구현 진행.** 근거 셋:
1. 감사자 자신이 설계 방향을 승인했고, 남은 지적이 전부 명세 정밀도이며 전건 반영됐다
2. 라운드 상한(6)에 도달했다 — 더 돌리면 수확 체감이 명확하다(r5·r6은 새 설계 위험 0건)
3. 남은 리스크는 **테스트 18개**가 받는다. 그중 10개가 "조용한 실패"를 잡는 것이다

**단 이는 형식 수렴이 아니라 리뷰어의 판단이다.** 무인 세션이었다면 규칙대로 정지했어야
한다. 사람이 판단할 수 있는 세션이라 다르게 결정했음을 기록한다.

## 6라운드 총계

지적 33건 · 전건 반영 · 기각 0건. 테스트 6개 → **18개**.
설계문서 302줄 → 600줄+.
